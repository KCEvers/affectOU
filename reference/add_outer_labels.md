# Add outer labels to a multi-panel plot

Add outer labels to a multi-panel plot

## Usage

``` r
add_outer_labels(P, main = NULL, xlab = NULL, ylab = NULL)
```

## Arguments

- P:

  Parameter list

- main:

  Main title

- xlab:

  X-axis label

- ylab:

  Y-axis label
