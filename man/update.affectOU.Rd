% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/affectOU-methods.R
\name{update.affectOU}
\alias{update.affectOU}
\title{Update model configuration}
\usage{
\method{update}{affectOU}(
  object,
  ndim = NULL,
  theta = NULL,
  mu = NULL,
  gamma = NULL,
  sigma = NULL,
  initial_state = NULL,
  ...
)
}
\arguments{
\item{object}{An object of class \link{affectOU}.}

\item{ndim}{Optional. New dimensionality of the affect process.}

\item{theta}{Optional. New attractor strength (scalar or matrix).}

\item{mu}{Optional. New attractor location (scalar or vector).}

\item{gamma}{Optional. New diffusion coefficient (scalar or matrix).}

\item{sigma}{Optional. New noise covariance (scalar or matrix).}

\item{initial_state}{Optional. New starting value of affect.}

\item{...}{Additional arguments (unused)}
}
\value{
Updated \link{affectOU} object
}
\description{
Modify the parameters and initial state of an Ornstein-Uhlenbeck (OU) model.
}
\examples{
# 1D model
model <- affectOU()
model_new <- update(model, mu = 1)

# 2D model
theta <- matrix(c(0.5, 0, 0, 0.3), nrow = 2)
model_2d <- affectOU(theta = theta, mu = 0, gamma = diag(2))
model_2d_new <- update(model_2d, mu = c(1, -1))
}
