% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{as.array.simulate_affectOU}
\alias{as.array.simulate_affectOU}
\title{Convert simulation results to array}
\usage{
\method{as.array}{simulate_affectOU}(x, ...)
}
\arguments{
\item{x}{A \code{simulate_affectOU} object}

\item{...}{Additional arguments (unused)}
}
\value{
A 3-dimensional array with dimensions:
\describe{
\item{time}{Time points (named by time values)}
\item{dim}{Dimension index}
\item{sim}{Simulation index}
}
}
\description{
Returns the raw simulation data as a 3-dimensional array with
dimensions (time × ndim × nsim).
}
\examples{
model <- affectOU(ndim = 2)
sim <- simulate(model, nsim = 3)
arr <- as.array(sim)
dim(arr)

# Access first time point across all dimensions and simulations:
arr[1, , ]
}
