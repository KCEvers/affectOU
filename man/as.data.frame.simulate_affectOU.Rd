% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{as.data.frame.simulate_affectOU}
\alias{as.data.frame.simulate_affectOU}
\title{Convert simulation results to data frame}
\usage{
\method{as.data.frame}{simulate_affectOU}(
  x,
  row.names = NULL,
  optional = FALSE,
  direction = c("long", "wide"),
  ...
)
}
\arguments{
\item{x}{A \code{simulate_affectOU} object}

\item{row.names}{NULL or a character vector giving the row names for the
data frame. Missing values are not allowed.}

\item{optional}{Logical. If TRUE, setting row names and converting column
names is optional. Included for compatibility with the generic.}

\item{direction}{Character, either \code{"long"} (default) or \code{"wide"}.
Long format has one row per time-dimension-simulation combination.
Wide format has one row per time point with dimensions and simulations
spread across columns.}

\item{...}{Additional arguments (unused)}
}
\value{
For \code{direction = "long"}, a data frame with columns:
\describe{
\item{time}{Observation time}
\item{dim}{Dimension index}
\item{sim}{Simulation index}
\item{value}{Simulated value}
}
For \code{direction = "wide"}, a data frame with \code{time} as the first column
and subsequent columns named \verb{dim\{d\}.sim\{s\}} (or simplified names for
univariate or single simulations).
}
\description{
Converts an \code{simulate_affectOU} object to a data frame in either long or
wide format.
}
\examples{
model <- affectOU(ndim = 2)
sim <- simulate(model, nsim = 3)

# Long format (default) - one row per timepoint, dimension, and simulation
df_long <- as.data.frame(sim)
head(df_long)

# Wide format - one row per time point
df_wide <- as.data.frame(sim, direction = "wide")
head(df_wide)
}
