% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{as.matrix.simulate_affectOU}
\alias{as.matrix.simulate_affectOU}
\title{Convert simulation results to matrix}
\usage{
\method{as.matrix}{simulate_affectOU}(x, direction = c("long", "wide"), ...)
}
\arguments{
\item{x}{A \code{simulate_affectOU} object}

\item{direction}{Character, either \code{"long"} (default) or \code{"wide"}.
Long format has one row per observation with columns for
time, dim, sim, and value. Wide format has time as rows and
dimension-simulation combinations as columns.}

\item{...}{Additional arguments (unused)}
}
\value{
For \code{direction = "long"}, a matrix with columns
\code{time}, \code{dim}, \code{sim}, \code{value}. For \code{direction = "wide"}, a matrix with dimensions
(ntime x ndim*nsim). Columns iterate over dimensions first, then
simulations.
}
\description{
Returns the simulation data as a 2-dimensional matrix in either long or
wide format.
}
\examples{
model <- affectOU(ndim = 2)
sim <- simulate(model, nsim = 3)

# Long format (default)
mat_long <- as.matrix(sim)
head(mat_long)

# Wide format
mat_wide <- as.matrix(sim, direction = "wide")
head(mat_wide)
}
