% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{as.list.simulate_affectOU}
\alias{as.list.simulate_affectOU}
\title{Convert simulation results to list}
\usage{
\method{as.list}{simulate_affectOU}(x, direction = c("wide", "long"), ...)
}
\arguments{
\item{x}{A \code{simulate_affectOU} object}

\item{direction}{Character string specifying the output format: \code{"wide"} (default)
returns one column per dimension, \code{"long"} returns a single value column
with a dimension indicator.}

\item{...}{Additional arguments (unused)}
}
\value{
A list of data frames, one per simulation. Each data frame contains:
\describe{
\item{time}{Time points}
\item{dim1, dim2, ...}{(wide format) Values for each dimension}
\item{dim}{(long format) Dimension indicator}
\item{value}{(long format) Simulated values}
}
}
\description{
Returns the simulation data as a list with one data frame per simulation.
}
\examples{
model <- affectOU(ndim = 2)
sim <- simulate(model, nsim = 3)

# Wide format (default): one column per dimension
lst_wide <- as.list(sim)
head(lst_wide[[1]])

# Long format: single value column with dimension indicator
lst_long <- as.list(sim, direction = "long")
head(lst_long[[1]])
}
